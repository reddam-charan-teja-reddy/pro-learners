import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	images: {
		domains: ['lh3.googleusercontent.com'], // Add the external hostname
	},
	// Add other config options here if needed
};

export default nextConfig;
